# SPDX-License-Identifier: GPL-2.0-only
#
# This subdirectory's source files are all linked into the single
# ipa-driver.ko module built by drivers/platform/msm/ipa/Makefile.
#
# Kbuild still descends here because the parent Makefile references
# files in this directory via `ipa_v2/foo.o` paths, and Kbuild resolves
# those by visiting the subdirectory. Keep this Makefile present (even
# empty) so Kbuild does not warn.
